<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>View Team Members</title>
  <%- include('../../partials/headlink.ejs') %>
</head>

<body>
  <%- include('../../partials/Header.ejs') %>
    <%- include("../../partials/sidebar.ejs") %>
      <%- include("../../partials/alertMsg.ejs") %>

      <main id="main" class="main">
        <div class="pagetitle">
          <h1><%- user.type %></h1>
          <nav>
            <ol class="breadcrumb">
              <li class="breadcrumb-item"><a href="/">Home</a></li>
              <li class="breadcrumb-item active">Team Management</li>
            </ol>
          </nav>
        </div>

        <section class="section dashboard">
          <div class="row">
            <div class="col-lg-12">
              <div class="card">
                <div class="card-body">
                  <h5 class="card-title">Filter Team Members</h5>
                  <form class="row g-3" action="/view-team-member">
                    <div class="col-md-4">
                      <label>Name</label>
                      <input type="text" name="name" class="form-control" value="<%= query.name || '' %>"
                        placeholder="Enter name">
                    </div>
                    <div class="col-md-4">
                      <label>Role</label>
                      <input type="text" name="role" class="form-control" value="<%= query.role || '' %>"
                        placeholder="Enter role">
                    </div>
                    <div class="col-md-2">
                      <label>Status</label>
                      <select name="status" class="form-select">
                        <option value="" <%= query.status === '' || typeof query.status === 'undefined' ? 'selected' : '' %>>All</option>
                        <option value="true" <%= query.status === 'true' ? 'selected' : '' %>>Active</option>
                        <option value="false" <%= query.status === 'false' ? 'selected' : '' %>>Inactive</option>
                      </select>
                    </div>
                    <div class="col-md-2 d-flex align-items-end">
                      <div>
                        <button type="submit" class="btn btn-primary">Submit</button>
                        <a href="/view-team-member" class="btn btn-secondary">Reset</a>
                      </div>
                    </div>
                  </form>
                </div>
              </div>

              <div class="card">
                <div class="card-body">
                  <div class="d-flex justify-content-between align-items-center">
                    <h5 class="card-title mb-0">Team Members</h5>
                    <a href="/team-member" class="btn btn-primary btn-sm">Add Team Member</a>
                  </div>
                  <div style="border-top: 1px solid #e9ecef; margin: 12px 0 14px 0;"></div>

                  <table class="table datatable">
                    <thead>
                      <tr>
                        <th>#</th>
                        <th>Image</th>
                        <th>Name</th>
                        <th>Role</th>
                        <th>Sort Order</th>
                        <th>Status</th>
                        <th>Action</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% let i = 0; %>
                        <% for (const member of data) { %>
                          <tr>
                            <td><%= ++i %></td>
                            <td>
                              <% const imageSrc = !member.image ? '/web/img/team-1.png' : (member.image.startsWith('http') || member.image.startsWith('/') ? member.image : (member.image.startsWith('team-') ? '/web/img/' + member.image : '/uploads/' + member.image)); %>
                                <img src="<%= imageSrc %>" alt="team" width="45" height="45"
                                  style="object-fit:cover;border-radius:50%;">
                            </td>
                            <td><%= member.name %></td>
                            <td><%= member.role %></td>
                            <td><%= Number(member.sortOrder || 0) %></td>
                            <td>
                              <% if (member.status) { %>
                                <span class="badge bg-success">Active</span>
                              <% } else { %>
                                <span class="badge bg-secondary">Inactive</span>
                              <% } %>
                            </td>
                            <td>
                              <a href="/team-member?id=<%= member._id %>" class="btn btn-info" data-bs-toggle="tooltip"
                                data-bs-placement="top" data-bs-title="Edit">
                                <i class="ri-edit-box-line"></i>
                              </a>
                              <a href="/delete-team-member?id=<%= member._id %>" class="btn btn-danger" data-bs-toggle="tooltip"
                                data-bs-placement="top" data-bs-title="Delete">
                                <i class="ri-delete-bin-5-line"></i>
                              </a>
                            </td>
                          </tr>
                        <% } %>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </section>
      </main>

      <%- include("../../partials/script.ejs") %>
</body>

</html>

